<?xml version="1.0" encoding="GBK"?>

<project default="zip" name="acrs" basedir=".">  
	<property file="build.properties" />
    <target name="zip" description="打包项目模板">
    	<zip destfile="${app.name}.zip"
    		basedir="."
    		excludes="**/.svn,${app.name}.zip,build_this.xml">
    	</zip>
    </target>

</project>
